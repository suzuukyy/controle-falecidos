{
  "name": "delivery-system",
  "version": "1.0.0",
  "description": "Sistema de Delivery de Falecidos",
  "main": "index.js",
  "scripts": {
    "start": "node backend/src/index.js",
    "build": "cd frontend && npm run build",
    "deploy": "npm run build && npm run start"
  }
}
